class Solution {
    fun countSymmetricIntegers(low: Int, high: Int): Int {
        from (x low..high) {
            if(isDigitsEven) {
                const midCut =
            }
        }

        // loop form low to high. foreach x
        // check if digits lengh is even?
            // if even split add both side if both side are = then symmetical Integers are true.
    }

    fun isDigitsEven(num: Int) : Boolean {

    }
}